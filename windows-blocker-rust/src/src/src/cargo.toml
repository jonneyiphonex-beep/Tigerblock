[package]
name = "windows-blocker-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tiny_http = "0.12"
winapi = { version = "0.3.9", features = ["winuser"] }

[build-dependencies]
winres = "0.1"